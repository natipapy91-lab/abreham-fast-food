{
  "name": "abreham-fast-food",
  "version": "1.0.0",
  "scripts": {
  "dev": "vite",
  "build": "vite build",   <-- OR "tsc && vite build"
  "lint": "eslint ...."
},
  "engines": {
  "node": "22.x"
}
}